import pandas as pd

def process_data(df):
    """
    Function to process data.

    Args:
        data (DataFrame): Data to be processed.

    Returns:
        DataFrame: Processed data.
    """
    # Operations related to data processing (e.g., calculations, cleaning, transformations)
    # ...
    # ...
    # ...

    # Returning the processed data
    return df

# +
# Use case
# connection = connect_to_oracle("my_username", "my_password", "localhost", "1521", "my_service")
# data = fetch_data(connection, "SELECT 1 FROM dual")
# processed_data = process_data(data)
